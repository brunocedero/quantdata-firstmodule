# Paquetes de dbt. dbt_utils trae tests y macros muy usados (unique_combination_of_columns, etc.)
# Tras agregarlo hay que correr `dbt deps` una vez para descargarlo.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]